<h1 class="title">Alphabetized Customers</h1>

<div class="buttons">
  <a href="<%= root_path %>" class="button is-light">All Customers</a>
  <a href="<%= customers_missing_email_path %>" class="button is-link">Customers Missing Email</a>
</div>

<div class="container">
  <% @customers.each do |customer| %>
    <%= render partial: "customers/customer", locals: { customer: customer } %>
  <% end %>
</div>